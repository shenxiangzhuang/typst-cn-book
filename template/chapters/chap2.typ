= 基础格式使用 <chapter2>

@chapter2[章节]介绍基本格式的使用。
#lorem(100)


== 代码
下面是一个Rust代码块示例:
```rust
fn main() {
    println!("Hello World!");
}
```

#lorem(100)

== 数学公式


我们定义$phi.alt$如下:
$ phi.alt := (1 + sqrt(5)) / 2 $ <ratio>

根据@ratio, 可以得到@fn:
$ F_n = floor(1 / sqrt(5) phi.alt^n) $ <fn>


=== 数学公式注解
#lorem(100)